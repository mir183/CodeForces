#include <stdio.h>
#include <string.h>
#include <ctype.h>
int main(){
    char c[500];
    gets(c);
    char cu=tolower(c);
    int len=strlen(cu);
    /*for(int i=cu[0];i<cu[len];i++){
        for(int j=cu[1];j<cu[len-1];j++){
            if()
        }
    }

}*/
}