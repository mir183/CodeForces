#include<stdio.h>
#include<string.h>
int main()
{
    int t;
    scanf("%d", &t);
    while (t>0){
        char s[1000];
        gets(s);
        int len=strlen(s);
        for(int i=0;i<len;i++){
            if (s[i]=='0'){
                s[i+1]==
            }
        }
    }
}